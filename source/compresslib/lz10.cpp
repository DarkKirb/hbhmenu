#include <fstream>
#include <string>
#include <sstream>
#include "compress.hpp"
using namespace std;/*
LZ10::LZ10(string fname) {
    stringstrame ss;
    ifstream f("romfs:/"+fname);
    uint8_t header;
    f >> header;
    if(header!=0x10)
        throw nullptr;
    int decompressedSize=0;
    
}
LZ10::~LZ10() {}
*/
